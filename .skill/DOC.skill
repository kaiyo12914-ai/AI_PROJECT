# 文件生成技能（DOC.skill v2）

Version: 2.1.0  
Last-Updated: 2026-05-25  
Owner: DOC 模組維護者  
Purpose: 定義 AI Agent 在公文草擬流程中的輸入、約束、檢核與輸出品質標準。

## 0. 適用範圍
- 適用模組：`webapps/doc`
- 適用文別：`order_draft`、`submit_draft`、`letter_draft`
- 本技能聚焦「生成前規則檢查」與「生成後品質校正」。

## 1. 輸入定義
- 必填欄位：
  - `doc_type`: `order_draft | submit_draft | letter_draft`
  - `stage2_facts`: 第二階段整理後事實
  - `incoming_level`: `from_direct_superior | from_superior | from_peer | from_subordinate | general`
- 選填欄位：
  - `incoming_text`
  - `reference_text`
  - `fixed_quote`
  - `signer_self_ref`（簽署者自稱，如「本局」、「本人」）

## 2. 強制規則（不得違反）
1. `fixed_quote` 出現時，內容不得改寫或刪節。
2. 引述區塊必須與正文語氣明確區隔。
3. 若有多來源事實，需先統整、再輸出，不得互相矛盾。
4. 不可憑空新增法條、機關名稱、日期、文號、附件編號。
5. 未提供事實時，不得杜撰結論或承諾事項。

## 3. 來文層級語氣規則
- `from_direct_superior | from_superior`：語氣正式、簡潔、明確回應指示。
- `from_peer`：語氣中性協調，避免命令式措辭。
- `from_subordinate`：語氣以審閱、核定、補正為主。
- `general`：採一般公文標準語氣，避免過度主觀敘述。

## 4. 生成流程建議
1. 先檢查輸入完整性與欄位合法值。
2. 將 `stage2_facts` 去重與排序。
3. 依文別套用段落框架（主旨、說明、辦法/擬辦）。
4. 套用來文層級語氣規則。
5. 檢查專有名詞、日期、附件與引述一致性。
6. 最後做可讀性與精簡修整，不改動既有事實。

## 5. 品質檢核
- 內容必須：
  - 有完整句意，避免破句與重複句
  - 無自我矛盾或前後衝突
  - 不含「模型自述」或「推理過程外露」
- 引述必須：
  - 可定位來源
  - 與原文語義一致
  - 不混入未提供事實

### 錯誤處理原則
- 若事實不足，應回報「缺少必要資訊」並指出缺口。
- 若欄位非法，應回報欄位名稱與允許值。
- 若輸入互相衝突，先列衝突點再請求確認。

## 6. 生成禁止事項
- 禁止捏造法規、機關、文號、會議決議。
- 禁止輸出「（略）」、「同上」等模糊替代句。
- 禁止把內部提示字串、測試標記、系統訊息輸出到草稿。
- 禁止未經授權的語氣升級（例如逕下命令）。

## 7. 輸出格式要求
- 盡量採固定段落模板（主旨／說明／辦法或擬辦）。
- 附件清單需具體、可對應、可追溯。
- 條列格式需一致（編號、標點、縮排一致）。

## 8. 去重與精簡策略
- 優先刪除語義重複句，不刪除新資訊句。
- 主旨保持單句可讀，避免過長複句。
- 說明段以「事實 -> 判斷 -> 建議」順序整理。

## 9. 生成前 QA 清單
- Q1：是否有 `fixed_quote`，且已原樣保留？
- Q2：是否有缺失必填欄位？
- Q3：`stage2_facts` 是否存在互斥資訊？
- Q4：來文層級語氣是否正確？
- Q5：是否含未提供之新事實？
- Q6：附件、日期、文號是否一致？
- Q7：最終文字是否通順且可直接送審？

## 10. 相關程式掛點
- `webapps/doc/views_generate.py`
  - `_check_quality_gate_readonly`
  - `_reduce_explain_point12_overlap`
  - `_ensure_explain_four_points`
  - `_final_strip_single_action_marker`
  - `_normalize_approval_phrase`
- `webapps/doc/views_parse.py`
  - `_extract_attachment_points`
  - `_extract_attachment_title`
- `webapps/doc/views_parse_subject.py`
  - `_extract_doc_subject`
  - `_extract_doc_subject_fallback`

備註：
- parse 階段先確保欄位與事實品質，再交給 generate 階段做語氣與段落輸出。
- 若來源品質不足，應先回報資料缺口，不應硬產生看似完整但不可用的草稿。
