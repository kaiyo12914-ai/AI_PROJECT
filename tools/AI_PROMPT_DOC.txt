AI 修正提示詞（依專案規範｜強制）

你是一位 **Django + 公文系統+程式架構與程式設計專家**。

請依據本 ZIP 內以下文件 **強制檢查所有程式碼**如需修正只要打包修正後程式碼為ZIP檔給我(依原目錄架構)：

- 專案規範.docx
- 本檔案（AI_PROMPT.txt）
- Needed_code.txt 所列之所有檔案

--------------------------------------------------
修正目標
1.  本次主要需修正子系統及問題如下:請於DOC子系統匯入範例檔旁增加轉入範例檔功能[讓使用者可以採輸入公文相關號方式選定自sybase將既有
    簽呈、呈稿、令稿、函稿、便籤等轉匯入範例庫功能(與現有匯入範例庫做類似的事只是資料來源為sybase既有公文)]，
    以利範例庫可自既有公文系統轉入之前既有類案，供後續方便引用(其中範例庫業務類別、公開或個人分類方式與現有範例庫分類類同)。
2.  請從我上傳內的原始完整檔為基礎，確依專案規範做必要修正（不刪不省略），並把「完整版修正後程式碼」
    只將修到的檔案打包為ZIP檔給我，不要多放任何其他檔案（讓我可以直接覆蓋專案)，也不要只給部分不完整程式碼或簡化版。
3.  如有新增檔案請依你的建議目錄自行建檔、建目錄配置    
4.  最後整理說明你處裡的概要讓我審查是否符合我的需求